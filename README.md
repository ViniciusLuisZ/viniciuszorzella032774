# viniciuszorzella032774
Projeto Full Stack

A documentação da API é disponibilizada via Swagger/OpenAPI em /swagger-ui.html, com endpoints versionados sob /api/v1.

O rate limit é aplicado por usuário autenticado (JWT), com fallback por IP para requisições não autenticadas.